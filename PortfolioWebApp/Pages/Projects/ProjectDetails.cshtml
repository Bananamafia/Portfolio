@page
@model PortfolioWebApp.Pages.Projects.ProjectDetailsModel
@{
    ViewData["Title"] = Model.selectedProject.Title;
}


<h2>@Model.selectedProject.Title</h2>
<h4 class="text-muted font-weight-lighter">@Model.selectedProject.StartingDate.ToString("MMM yyy") - @Model.selectedProject.EndingDate.ToString("MMM yyy")</h4>

<p class="font-italic">@Model.selectedProject.Description</p>


<div class="row">
    <div class="col-md-9">
        <img src=@Model.selectedProject.Image />
    </div>

    <div class="col-md-3">
        @if (Model.selectedProject.CodeLink != null)
        {
            <h2><span class="badge badge-secondary"><a href=@Model.selectedProject.CodeLink class="text-white"><i class="fab fa-github"></i> zum Code</a></span></h2>
            <br />
        }

        @if (Model.selectedProject.UsedTools != null)
        {
            <h5>Verwendete Tools:</h5>
            <ul>
                @foreach (var tool in Model.selectedProject.UsedTools)
                {
                    <li>@tool</li>
                }
            </ul>
        }
    </div>




</div>





@*<div id="carousel" class="carousel slide bg-dark" data-ride="carousel">
        <ol class="carousel-indicators">
            <li data-target="#carousel" data-slide-to="0" class="active"></li>
            <li data-target="#carousel" data-slide-to="1"></li>
            <li data-target="#carousel" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="~/Images/SampleImage.png" class="d-block mx-auto">
            </div>
            <div class="carousel-item">
                <img src="~/Images/SampleImage.png" class="d-block mx-auto">
            </div>
            <div class="carousel-item">
                <img src="~/Images/SampleImage.png" class="d-block mx-auto">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carousel" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carousel" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>*@

